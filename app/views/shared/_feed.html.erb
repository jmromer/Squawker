<% if @feed_items.any? %>
  <ul class="squawks">
    <%= render partial: 'shared/feed_item', collection: @feed_items %>
  </ul>

  <div class="paginate">
    <%= will_paginate @feed_items, page_links: false %>
  </div>

<% end %>